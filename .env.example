MODE=LOCAL
LOG_LEVEL=INFO
POSTGRES_DB=artshop
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
# For local dev: localhost. For Docker prod: use 'db' and 'redis'
DB_HOST=localhost
DB_PORT=5432
REDIS_HOST=localhost
REDIS_PORT=6379
JWT_SECRET_KEY=change-this-secret-key-in-production
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=30
JWT_REFRESH_TOKEN_EXPIRE_DAYS=7
# false for local HTTP dev; set to true in .env.prod (HTTPS)
COOKIE_SECURE=false
ADMIN_EMAILS=["admin@example.com"]

# Frontend API base.
# Local/dev: localhost. Production Docker build uses docker-compose.prod.yml -> http://api:8000.
# Browser requests still use same-origin /api through nginx; this value is for rewrites/SSR.
NEXT_PUBLIC_API_URL=http://localhost:8000

# Google OAuth (Frontend: passed as Docker build arg; Backend: validates token)
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com

# Google Maps Places API (optional, enables address autocomplete in checkout)
# Get key from: https://console.cloud.google.com/apis/credentials
# Enable "Places API" in the Google Cloud Console
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=

# --- PostHog Analytics ---
# Get from: https://eu.posthog.com (Project Settings â†’ Project API Key)
# Use EU cloud for GDPR compliance: https://eu.i.posthog.com
NEXT_PUBLIC_POSTHOG_KEY=phc_xxxxxxxxxxxxxxxxxxxx
NEXT_PUBLIC_POSTHOG_HOST=https://eu.i.posthog.com

# Tracking: Dozzle log viewer password (only needed in .env.prod on server)
DOZZLE_PASSWORD=change-this-in-production

# --- EMAIL SETTINGS (SMTP) ---
# Settings for sending emails (the "bot / courier" account).
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_USER=technical_sender_bot@gmail.com
SMTP_PASSWORD=your_16_character_app_password

# --- Monobank Acquiring (Payment Gateway) ---
# Test token from https://api.monobank.ua/ (for testing only).
# Production token from https://web.monobank.ua/ (requires Ð¤ÐžÐŸ/business account).
MONOBANK_TOKEN=your_monobank_token_here
# Public HTTPS URL for Monobank to POST payment status webhooks.
# Production nginx exposes FastAPI under /api, so keep the /api prefix here.
MONOBANK_WEBHOOK_URL=https://samen-bondarenko.com/api/payments/webhook
# URL to redirect buyer after payment (success or failure page).
MONOBANK_REDIRECT_URL=https://samen-bondarenko.com/checkout/success

# --- Prodigi Print-on-Demand ---
# API key from https://dashboard.prodigi.com/settings/api
PRODIGI_API_KEY=your_prodigi_api_key_here
# true uses the sandbox API and is never charged/fulfilled by Prodigi.
# false uses the live production API and requires billing details in the Prodigi account.
PRODIGI_SANDBOX=true
# Public HTTPS backend URL used in Prodigi callback and asset URLs.
PUBLIC_BASE_URL=https://samen-bondarenko.com
# Optional shared secret added to Prodigi callback URLs and checked on receipt.
PRODIGI_WEBHOOK_SECRET=change-this-prodigi-webhook-secret
# Public storage for rendered print-order PNGs that Prodigi downloads.
# Keep "local" for basic dev. Set "s3_compatible" for real sandbox/live order submits.
# AWS S3 example:
#   PRINT_ASSET_BUCKET=artshop-prodigi-assets
#   PRINT_ASSET_REGION=eu-north-1
#   PRINT_ASSET_PUBLIC_BASE_URL=https://artshop-prodigi-assets.s3.eu-north-1.amazonaws.com
#   PRINT_ASSET_ENDPOINT_URL is empty for AWS S3; use it only for S3-compatible providers.
PRINT_ASSET_STORAGE_BACKEND=local
PRINT_ASSET_BUCKET=artshop-prodigi-assets
PRINT_ASSET_ENDPOINT_URL=
PRINT_ASSET_REGION=eu-north-1
PRINT_ASSET_ACCESS_KEY_ID=your_print_asset_access_key_id
PRINT_ASSET_SECRET_ACCESS_KEY=your_print_asset_secret_access_key
PRINT_ASSET_PUBLIC_BASE_URL=https://artshop-prodigi-assets.s3.eu-north-1.amazonaws.com
PRINT_ASSET_PREFIX=prodigi
# Dev-only raw 4GB supplier CSV dump used to generate the small committed source.
PRODIGI_RAW_CSV_ROOT=Prodigy
# Committed curated CSV used by production snapshot/payload rebuilds.
PRODIGI_CURATED_CSV_PATH=src/integrations/prodigi/data/prodigi_storefront_source.csv
# Inactive storefront bakes to keep after each successful rebuild. Active bake is always kept.
PRODIGI_STOREFRONT_BAKE_RETENTION=2

# --- Telegram Bot Integration ---
# Create your bot at https://t.me/BotFather -> /newbot
# Used only for owner alerts when a customer places an order.
TELEGRAM_BOT_TOKEN=your_bot_token_from_botfather
# Your personal Telegram chat_id for new-order notifications.
# Get it: message @userinfobot in Telegram -> it replies with your chat_id.
TELEGRAM_ADMIN_CHAT_ID=your_chat_id_here
